#!/bin/sh

set -eux
scriptDir=$(dirname "$0")
rootDir="$(dirname "$scriptDir")"
dir="$rootDir/var/log"
file=$dir/prod.log

if [ -f $file ]; then
  sudo mv $file $dir/$(date +"%Y-%m-%d--%H-%M-%S").prod.log
  :>> $file
  chown 989:33 $file
fi